DATABASE_URL=postgresql://user:password@host/dbname          # reused AusRegBench Neon pgvector connection string, read-only
OPENAI_API_KEY=                                                # used only to embed search_provision query text, matches AusRegBench's text-embedding-3-large corpus embeddings
AWS_REGION=ap-southeast-2
CLAUDE_CODE_USE_BEDROCK=1
